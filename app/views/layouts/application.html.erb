<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->

    <title>Hive Mind</title>
    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
    <% Rails::Engine.subclasses.map{|e| e.name.split('::')[0].underscore}.select{|e| e =~ /^hive_mind_/}.each do |e| %>
      <% if Rails.application.assets.find_asset "#{e}.js" %>
        <%= javascript_include_tag e %>
      <% end %>
    <% end %>
  
    <%= csrf_meta_tags %>

    <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
      <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->

    <% if automatic_refresh %>
      <script>setTimeout("location.reload(true);",<%= automatic_refresh %>);</script>
    <% end %>

</head>
<body>
  
<div class="container-fluid">

  <%= render partial: "layouts/navigation" %>

  <div class="col-md-12">
    <%= flash_messages(:close, :fade) %>
    <%= yield %>
  </div>

</div>

<footer class="footer">
  <div class="container">
    <p class="text-muted">Version: <%= HiveMind::Application::VERSION %></p>
  </div>
</footer>
</body>
</html>
